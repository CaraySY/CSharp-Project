using System;

namespace Collections
{
    class Program
    {
        static void Main(string[] args)
        {
            /*Contain contain = new Contain();
            contain.Init();
            contain.Print();
            Console.WriteLine("please input remove data:");
            int temp = Convert.ToInt32(Console.ReadLine());
            contain.Remove(temp);
            Console.WriteLine("====================================================");
            Genericity<int> genericity = new Genericity<int>(10, 20);
            Console.WriteLine("sum="+genericity.Sum());*/

        }
    }
}
